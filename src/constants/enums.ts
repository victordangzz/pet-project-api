export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned,
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken,
}

export enum BlogStatus {
  Draft = "draft",
  Published = "published",
  Archived = "archived",
}
